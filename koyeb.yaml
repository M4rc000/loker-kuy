name: lokerkupy-backend
service:
  name: backend
  instance: free
  ports:
    - port: 8080
      protocol: http
  routes:
    - path: /
  env:
    - key: PORT
      value: "8080"
    - key: CORS_ORIGIN
      value: "https://lokerkupy.vercel.app"
    - key: FIREBASE_DATABASE_URL
      secret: FIREBASE_DATABASE_URL
    - key: FIREBASE_SECRET
      secret: FIREBASE_SECRET
  docker:
    dockerfile: backend/Dockerfile
    context: .
